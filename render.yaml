services:
  - type: web
    name: yango-api
    env: node
    rootDir: backend
    buildCommand: yarn install && yarn build && cd backend && yarn prisma generate
    startCommand: cd backend && yarn start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
